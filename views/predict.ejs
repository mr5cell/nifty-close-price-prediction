<!DOCTYPE html>
<html>
<head>
    <title>Submit Prediction</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: monospace; background: #000; color: #0f0; padding: 20px; }
        .container { max-width: 500px; margin: 0 auto; }
        h1 { font-size: 24px; margin-bottom: 20px; border-bottom: 2px solid #0f0; padding-bottom: 10px; }
        .info { background: #111; padding: 15px; margin-bottom: 20px; border: 1px solid #0f0; }
        .info p { margin: 5px 0; }
        form { background: #111; padding: 20px; border: 1px solid #0f0; }
        .form-group { margin-bottom: 15px; }
        label { display: block; margin-bottom: 5px; }
        input { width: 100%; padding: 8px; background: #000; color: #0f0; border: 1px solid #0f0; font-family: monospace; }
        input:focus { outline: none; border-color: #0a0; }
        .btn { padding: 10px 20px; background: #0f0; color: #000; border: none; cursor: pointer; font-family: monospace; }
        .btn:hover { background: #0a0; }
        .error { background: #200; padding: 10px; border: 1px solid #f00; color: #f00; margin-bottom: 15px; }
        .success { background: #020; padding: 10px; border: 1px solid #0f0; color: #0f0; margin-bottom: 15px; }
        .back-link { display: inline-block; margin-top: 20px; color: #0f0; }
    </style>
</head>
<body>
    <div class="container">
        <h1>SUBMIT PREDICTION</h1>
        
        <div class="info">
            <% if (lastClosePrice) { %>
                <p>LAST CLOSE: <%= lastClosePrice.toFixed(2) %></p>
                <p>VALID RANGE: <%= (lastClosePrice * 0.7).toFixed(2) %> - <%= (lastClosePrice * 1.3).toFixed(2) %></p>
                <p>(±30% OF LAST CLOSE)</p>
            <% } else { %>
                <p>FETCHING PRICE DATA...</p>
            <% } %>
        </div>
        
        <form method="post" action="/predict">
            <% if (error) { %>
            <div class="error"><%= error %></div>
            <% } %>
            
            <% if (success) { %>
            <div class="success"><%= success %></div>
            <% } %>
            
            <div class="form-group">
                <label for="name">NAME:</label>
                <input type="text" id="name" name="name" required maxlength="50">
            </div>
            
            <div class="form-group">
                <label for="predicted_value">PREDICTED VALUE:</label>
                <input type="number" id="predicted_value" name="predicted_value" step="0.01" required>
            </div>
            
            <button type="submit" class="btn">SUBMIT</button>
        </form>
        
        <a href="/" class="back-link">← BACK TO LEADERBOARD</a>
    </div>
</body>
</html>