<!DOCTYPE html>
<html>
<head>
    <title>Admin Login</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: monospace; background: #000; color: #0f0; display: flex; justify-content: center; align-items: center; height: 100vh; }
        .login-box { background: #111; padding: 30px; border: 2px solid #0f0; width: 300px; }
        h1 { font-size: 20px; margin-bottom: 20px; text-align: center; }
        .form-group { margin-bottom: 15px; }
        label { display: block; margin-bottom: 5px; }
        input { width: 100%; padding: 8px; background: #000; color: #0f0; border: 1px solid #0f0; font-family: monospace; }
        input:focus { outline: none; border-color: #0a0; }
        .btn { width: 100%; padding: 10px; background: #0f0; color: #000; border: none; cursor: pointer; font-family: monospace; }
        .btn:hover { background: #0a0; }
        .error { background: #200; padding: 10px; border: 1px solid #f00; color: #f00; margin-bottom: 15px; text-align: center; }
    </style>
</head>
<body>
    <div class="login-box">
        <h1>ADMIN ACCESS</h1>
        
        <form method="post" action="/admin/login">
            <% if (error) { %>
            <div class="error"><%= error %></div>
            <% } %>
            
            <div class="form-group">
                <label for="pin">PIN:</label>
                <input type="password" id="pin" name="pin" required autofocus>
            </div>
            
            <button type="submit" class="btn">LOGIN</button>
        </form>
    </div>
</body>
</html>